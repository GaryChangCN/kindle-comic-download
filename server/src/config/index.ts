import { SupportType } from "../typings"

const config = {
    port: 2333,
    support: {
        baidu: {
            netease: true,
            tecent: true,
            dmzj: true
        }
    }
}

export default config
